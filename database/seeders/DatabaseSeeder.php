<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * The single user account is created by `php artisan laradooit:install`,
     * never by a seeder. A demo seeder for accounts, categories and a month of
     * transactions arrives with the domain tables.
     */
    public function run(): void
    {
        //
    }
}
